fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
pillow>=10.0
numpy>=1.24
# Stripe Checkout for token purchases (optional at runtime; only needed when
# STRIPE_SECRET_KEY is set). Safe to install everywhere.
stripe>=9.0
